- [Work via pull requests](work-via-pull-requests.md) — branch and open a PR; don't commit to master. `gh` lives at `C:\Users\mizpe\bin\gh\bin` (never a temp dir); MSI installs fail on this box until it is rebooted
- [Notes live in the repo](notes-live-in-the-repo.md) — memories and plans belong in `<repo>/.claude/`, git-tracked, not the per-user store
- [Stop tracking after tests](stop-tracking-after-tests.md) — switch tracking off when a test or session ends; left on, the mount drifts toward the western HA and altitude limits unattended
- [Park position](park-position.md) — Dec +66° on the meridian (Alt 54.61°, Az 0°); **`Park()` lands ~15′ west of it** because `parkingRa` is sampled before the slew — target `LST + slew duration` instead
- [Soft limits are not conservative](soft-limits-are-not-conservative.md) — **a physical limit switch was reached at HA −6.7255 while `eastern_haLimit` is −7.0.** The fixed HA bounds ignore declination, so passing them does not mean the position is reachable. Recovery from a `HardLimit` trip is an on-site job
- [HA angle distance is broken](ha-angle-distance-is-broken.md) — **RESOLVED 2026-09-20.** Two faults, not one: a 15× distance error in `FromRadians` and a direction that reached the wrong motor. Both fixed, both covered by `TestAngleHa`, and hour-angle slews now run on sky to 0.02′
- [slew-to-ha-dec is broken](slew-to-ha-dec-broken.md) — **RESOLVED.** Case-sensitivity and a `Substring` length bug in the parameter parsing, then the two faults above; the Action and the Dash's HA/Dec button both work
- [Common.dll shadowing](common-dll-shadowing.md) — **rebuilding `Common` or `Hardware` is not enough.** Every driver carries its own copy, they share one assembly identity, and the first loaded wins for the whole process, so a fresh build can be silently ignored at runtime. The deploy now syncs all x86 copies
- [Telescope drive topology](telescope-drive-topology.md) — TeleSlew is a shared speed selector; both axes must use the same speed among slew/set, only guide is independent
- [Slew time: where it goes](slew-time-where-it-goes.md) — where slew time goes on both axes, verified on sky. **HA coast is direction dependent and now tuned (east slews 73 % faster); Dec's southward coast varies 27 % and cannot be tuned the same way.** The rendezvous is cheap. Two measurement traps: the detector is blind at guide rate, and `stopping distance` lies on Dec
- [Renishaw encoder calibration](renishaw-encoder-calibration.md) — calibrated against the sky 2026-09-16: no scale error, zero point corrected, now the encoders in use; what's left is a pointing model
- [Wise40 ACP/driver integration](wise40-acp-driver-integration.md) — ACP won't pass Action through, DriverAccess isn't COM-creatable, and only one ProgID is safe to use
- [Wise40 build and environment gotchas](wise40-build-and-environment-gotchas.md) — COM registration needs elevation; the watcher gives children a broken environment; logs roll at noon UT. **Partly corrected 2026-09-21**: the solution build works now and is what the deploy uses, and a rebuild no longer costs settings
- [Secrets move to vault.json](secrets-move-to-vault-json.md) — **planned, not done.** MySQL root password and the PAT go to `c:\Wise40\vault.json`, outside git. **Relocating is not rotating**: the password is already in the history of a public repo, so it must be rotated; and `c:\Wise40` is non-admin writable, so the file needs its own ACL
- [Settings live in settings.json](settings-live-in-json.md) — since 2026-09-21 driver settings are in `c:\Wise40\settings.json`, not the ASCOM Profile. Registration still is; values now survive the rebuild that used to wipe them; the root sub-key is `(root)` so PowerShell can parse the file
- [Global\ objects across accounts](global-mutex-across-accounts.md) — the watcher is LocalSystem and the hubs run as the user, so a `Global\` mutex needs an explicit DACL. Created in a static initializer it killed every driver in the process, and it **only reproduces across accounts** — an in-process test proves nothing
- [No safe unelevated solution build](no-safe-unelevated-solution-build.md) — `/t:Build`, `/t:Compile` and even `RegisterForComInterop=false` all reach regasm; the `false` setting **unregisters** drivers, and its access-denied error hides the half that already succeeded. Take a `reg export` before any build
- [No AnyCPU consumers](no-anycpu-consumers.md) — everything the chain loads is x86. A configuration *named* `Debug|AnyCPU` can still target x86, so read `PlatformTarget` or the built assembly, never the configuration name
- [Diagnostics that break what they measure](diagnostics-that-break-what-they-measure.md) — `tail -f` locks a log on Windows and silences the writer, `Start-Process -PassThru` returns a null ExitCode, enumerating all COM classes takes minutes. Suspect the observation before the tool
- [Wise40 driver Connected/Dispose scoping](wise40-driver-connected-dispose.md) — per-client teardown used to reach shared singletons and stop the mount; fixed in six drivers, two left
- [Wise40 filter metadata sources](wise40-filter-metadata-sources.md) — names from MaxIm DL 7, offsets from ACP; Wise40 keeps only the RFID tags
- [Wise40 filter wheel Arduino source](wise40-filterwheel-arduino-source.md) — three copies exist; the live one is in Documents\Arduino, not the ASCOM.Wise40 solution tree
- [Filter wheel firmware validated on hardware](filterwheel-fixes-awaiting-hardware-test.md) — works end to end as of 2026-08-15; the slit detector needs ~40ms to settle or it lies convincingly
- [Filter wheel RFID stays on SoftwareSerial](filterwheel-rfid-stays-on-softwareserial.md) — rewiring D0 to pin 19 for hardware Serial1 was considered and rejected; don't propose it again
